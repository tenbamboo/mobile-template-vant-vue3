export default function addLoading(config) {
  console.log('test addLoaidng');
  // const storeInstance = getStore();
  // if (storeInstance && config && config.showLoading !== false) {
  //   storeInstance.commit(STORE_KEY.ADD_LOADING_COUNT);
  // }
  return config;
}
